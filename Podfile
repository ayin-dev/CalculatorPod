# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

# if there is 3rd party lib which CalculatorLibrary depends on, you need Specs
source 'https://github.com/CocoaPods/Specs.git'

source 'https://github.com/ayin-dev/CalculatorLibrary.git'

target 'CalculatorPod' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for CalculatorPod
  # With 'source' above, you can add release version
  pod 'CalculatorLibrary', '1.2'

  # If no source above, without :tag or :branch, if you be whatever in main
  #pod 'CalculatorLibrary', :git => 'https://github.com/ayin-dev/CalculatorLibrary.git'

end
